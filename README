Implemented a full-fledged HTTP server for static content for different Mime Types supporting HTTP 1.0/1.1 with all headers and status codes. Also built a TomCat like Servlet Engine for dynamic web content with support for cookies and sessions.